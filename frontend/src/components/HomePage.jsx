import { Link } from "react-router-dom";

const HomePage = () => {
    return (
        <div>
            HomePage <Link to="/signup"> Signup</Link>
        </div>
    );
};

export default HomePage;
